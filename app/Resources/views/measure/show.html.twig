{% extends 'base.html.twig' %}

{% block body %}
    <h1>Measure</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ measure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ measure.name }}</td>
            </tr>
            <tr>
                <th>Shortname</th>
                <td>{{ measure.shortName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('measure_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('measure_edit', { 'id': measure.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
